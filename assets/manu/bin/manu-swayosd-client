#!/bin/bash

# manu:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# manu:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(manu-hyprland-monitor-focused)" "$@"
