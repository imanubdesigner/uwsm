#!/bin/bash

# manu:summary=Launch a TUI command in the default terminal with Omarchy styling
# manu:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.manu.$(basename $1) -e "$1" "${@:2}"
