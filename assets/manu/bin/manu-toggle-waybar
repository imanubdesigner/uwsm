#!/bin/bash

# manu:summary=Toggle Waybar visibility
# manu:examples=manu toggle waybar

manu-toggle waybar-off
if pgrep -x waybar >/dev/null; then
  pkill -x waybar
else
  uwsm-app -- waybar >/dev/null 2>&1 &
fi
