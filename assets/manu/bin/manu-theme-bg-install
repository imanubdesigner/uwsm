#!/bin/bash

CURRENT_THEME_NAME=$(cat "$HOME/.config/manu/current/theme.name")
THEME_USER_BACKGROUNDS="$HOME/.config/manu/backgrounds/$CURRENT_THEME_NAME"

mkdir -p "$THEME_USER_BACKGROUNDS"
nautilus "$THEME_USER_BACKGROUNDS"
