#!/bin/bash

# manu:summary=Show current theme
# manu:examples=manu theme current

THEME_NAME_PATH="$HOME/.config/manu/current/theme.name"
if [[ -f $THEME_NAME_PATH ]]; then
  cat $THEME_NAME_PATH | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g'
else
  echo "Unknown"
fi
