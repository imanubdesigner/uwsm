#!/bin/bash

WALL="$1"

[ -z "$WALL" ] && exit 1

cp "$WALL" ~/.local/share/manu/current_wallpaper

matugen image "$WALL" --mode dark --source-color-index 0 --resize-filter lanczos3 --lightness-dark -0.04 --contrast 0.4
