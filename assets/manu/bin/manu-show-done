#!/bin/bash

# manu:summary=Display a "Done!" message with a spinner and wait for user to press any key.

echo
gum spin --spinner "dot" --title "Done! Press any key to close..." -- bash -c 'read -n 1 -s'
