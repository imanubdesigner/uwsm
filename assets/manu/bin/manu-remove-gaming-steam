#!/bin/bash

# manu:summary=Remove Steam and all of its game libraries, configs, and caches.
# manu:requires-sudo=true

set -e

manu-pkg-drop steam

rm -rf \
  "$HOME/.steam" \
  "$HOME/.local/share/Steam" \
  "$HOME/.config/steam" \
  "$HOME/.cache/steam"

echo ""
echo "Steam and its data have been removed."
